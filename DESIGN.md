# DrCT Asset Office Design Guide

> 기준일: 2026-07-21
> 이 문서는 현재 React 화면의 UX와 시각 규칙을 설명합니다. 시스템 구조는 [DrCT_ARCHITECTURE.md](./DrCT_ARCHITECTURE.md)를 참고합니다.

## 1. 제품 성격

DrCT Asset Office는 반복 조회, 비교, 기록, 훈련이 중심인 개인 투자 리서치 도구입니다. 화면은 장식보다 정보 밀도와 판단 속도를 우선합니다.

- 첫 화면부터 실제 업무 도구를 제공한다.
- 출처, 기준일, 수집 상태, 훈련 상태를 가까운 위치에 표시한다.
- 같은 의미의 값과 행동은 모든 화면에서 같은 색과 용어를 사용한다.
- 분석 지원과 사용자의 최종 판단을 구분한다.
- 실제 주문처럼 오해할 수 있는 표현에는 `가상`, `훈련`, `예상` 상태를 명확히 표시한다.

## 2. 화면 구조

### 애플리케이션 셸

- 좌측 내비게이션은 업무 영역을 기준으로 그룹화한다.
- 본문은 페이지 제목, 핵심 행동, 상태 메시지, 작업 영역 순으로 배치한다.
- 페이지 단위 섹션은 떠 있는 장식 카드보다 전체 폭의 작업 영역으로 구성한다.
- 반복 항목, KPI, 개별 종목과 같은 독립 단위에만 카드를 사용한다.

### 정보 위계

1. 페이지 또는 모달 제목
2. 현재 대상: 종목, 계좌, 기준일, 조건식
3. 주요 상태와 핵심 수치
4. 작업 컨트롤
5. 상세 데이터, 차트, 이력

제목은 작업 면적에 맞춘 크기를 사용한다. 모달과 사이드 패널 내부에서 과도한 대형 타이포그래피를 사용하지 않는다.

## 3. 시각 언어

### 기본 팔레트

| 역할 | 기준 색상 | 사용처 |
| --- | --- | --- |
| Ink | `#0f172a` | 본문, 제목, 기본 아이콘 |
| Muted | `#64748b` | 보조 설명, 메타데이터 |
| Border | `#e2e8f0` | 구분선, 입력 테두리 |
| Surface | `#ffffff` | 주 작업면 |
| Subtle surface | `#f8fafc` | KPI, 보조 패널 |
| Primary | `#150f23` | 핵심 실행 버튼 |
| Active blue | `#2563eb` | 선택, 포커스, 진행 중 도구 |
| Active blue bg | `#dbeafe` 또는 `#eff6ff` | 활성 상태 배경 |
| Positive/상승 | `#dc2626` 계열 | 한국 주식 관례의 상승·수익 |
| Negative/하락 | `#2563eb` 계열 | 하락·손실·손절 계획 |
| Success | `#047857` | 완료, 정상 처리 |
| Warning/Error | `#be123c` 계열 | 실패, 주의, 파괴적 행동 |

색상만으로 상태를 전달하지 않는다. 텍스트, 아이콘, 선 종류 또는 배지를 함께 사용한다.

### 형태와 간격

- 기본 모서리 반경은 `8px` 이하를 우선한다.
- 입력, 버튼, 탭의 높이는 같은 작업 행에서 일치시킨다.
- 카드 안에 다시 장식용 카드를 중첩하지 않는다.
- 고정 형식 차트, 툴바, KPI 그리드는 `minmax`, 고정 높이, `aspect-ratio` 등으로 크기 변화를 제어한다.
- 글자 간격은 `0`을 사용하며 뷰포트 너비에 따라 글자 크기를 직접 확대하지 않는다.

## 4. 공통 컴포넌트

### 버튼

- 명확한 명령에만 텍스트 버튼을 사용한다.
- 닫기, 삭제, 정렬, 차트 도구처럼 익숙한 동작은 Lucide 아이콘을 사용하고 툴팁 또는 접근성 이름을 제공한다.
- 핵심 실행은 짙은 Primary, 선택된 도구와 편집 중 상태는 Active blue를 사용한다.
- 삭제와 매도 실행은 위험 색을 사용하되 서로 다른 의미를 라벨로 구분한다.
- 비활성 버튼은 대비를 낮추고 클릭할 수 없음을 커서와 `disabled` 속성으로 함께 표현한다.

### 탭과 세그먼트

- 화면 전환은 탭, 상호 배타적 모드는 세그먼트 컨트롤을 사용한다.
- 활성 항목은 테두리, 배경, 글자색 중 둘 이상으로 식별한다.
- 탭 전환으로 입력값이 사라지는 경우 저장 여부 또는 변경 상태를 먼저 처리한다.

### 입력 폼

- 라벨은 입력 위에 배치하고 단위는 라벨 또는 값 가까이에 둔다.
- 숫자 입력의 글자 크기는 라벨과 과도한 차이가 나지 않게 한다.
- 원화, 퍼센트, 수량은 표시 단계에서 포맷하되 API 계약은 숫자 타입을 유지한다.
- 유효성 오류는 해당 입력 가까이에 구체적으로 표시한다.
- 체크박스, 토글, 선택 메뉴, 슬라이더는 값의 성격에 맞춰 사용한다.

### 표와 목록

- 스캔이 중요한 수치 열은 정렬하고 고정된 포맷을 사용한다.
- 종목명과 종목코드, 기준일과 상태처럼 함께 판단하는 정보는 가까이 배치한다.
- 로딩, 빈 결과, 오류를 각각 별도 상태로 제공한다.
- 목록 행 전체가 클릭 가능하면 내부 버튼과 이벤트가 충돌하지 않게 한다.

### 상태 메시지

- 성공은 초록, 정보는 파랑, 오류는 빨강 계열의 얕은 배경을 사용한다.
- 메시지는 수행한 작업과 다음 행동을 짧게 설명한다.
- 수집 완료 메시지에는 신규·갱신·전체 건수 등 확인 가능한 결과를 포함한다.

## 5. 차트

- 차트는 분석 대상 그 자체이며 장식 프레임 안에 축소하지 않는다.
- 캔들, 이동평균, 거래선, 계획선의 색과 범례를 일치시킨다.
- 한국 주식 화면의 상승/하락 색 관례를 유지한다.
- 계획선은 캔들을 가리지 않는 점선과 우측 라벨을 사용한다.
- 라벨이 가까우면 수직 간격을 조정해 겹침을 피한다.
- 차트 도구 버튼은 `수평선`, `추세선`, `코스피`, `코스닥`, `리스크관리`처럼 현재 기능을 직접 명명한다.
- 선택 모드는 버튼의 활성 색, 차트 상단 상태 문구, 포인터 변화로 동시에 알린다.

### 리스크 계획선

| 유형 | 색상 | 의미 |
| --- | --- | --- |
| 분할매수 | 보라 | 진입 계획 |
| 익절 | 빨강 | 수익 실현 계획 |
| 손절 | 파랑 | 손실 제한 계획 |

- 각 계획선은 안정적인 식별자를 가지며 칩의 `X`는 선택한 선만 삭제한다.
- 손절과 익절은 별도 유형으로 유지하고, 전량손절과 분할손절 규칙을 혼합하지 않는다.
- 저장 이후 메인 차트가 최신 계획을 다시 조회하도록 갱신 경계를 명확히 한다.

## 6. 모달과 패널

- 모달 헤더, 스크롤 본문, 하단 액션 영역을 분리한다.
- 하단 액션 영역은 본문을 덮지 않는다. 고정 또는 sticky라면 본문에 동일한 하단 여백을 확보한다.
- 긴 모달은 내부 본문만 스크롤하고 닫기 버튼과 최종 행동은 항상 접근 가능하게 한다.
- `취소`는 보조 버튼, 생성·저장·실행은 우측의 주 버튼으로 배치한다.
- 입력값이 남아 있을 때 닫기 동작이 데이터를 조용히 유실하지 않게 한다.

### 매수 주문 모달

`리스크 계획` 탭은 다음 순서를 유지한다.

1. 계획 가격 지정 도구와 계획 가격 차트
2. 주문 계획 및 리스크 요약
3. 수익·손절 시나리오와 메모

- `분할매수 가격 지정`, `분할매도/손절 가격 지정`은 작업 모드다.
- 작업 중 나타나는 `지정 완료` 버튼은 Active blue로 명확히 구분한다.
- `계획 저장 후 주문 입력`은 서버 저장 성공 뒤 주문 입력 탭으로 이동한다.
- 주문 실행 전 주문 대비 리스크 경고를 표시하되 가상 주문임을 유지한다.

### 매도 주문 모달

- 보유수량, 평균단가, 예상 반환현금과 손익을 주문 입력보다 먼저 보여준다.
- 매도 사유 입력 영역 아래에 충분한 공간을 확보하고 하단 버튼이 입력 영역을 가리지 않게 한다.
- 리스크 계획 단계가 있으면 현재 선택 단계와 예상 체결 후 잔여 계획을 함께 보여준다.

### 계좌관리매매 훈련

- 하나의 훈련계좌는 여러 종목 세션을 가질 수 있다.
- 계좌 상단에는 `신규매매`와 `계좌 설정`을 제공한다.
- 계속하기는 계좌 전체가 아니라 종목 카드의 `매매 계속하기`에서 수행해 대상 종목을 명확히 한다.
- 진행 중 매매와 완료 거래는 탭과 건수 배지로 구분한다.

## 7. 반응형과 접근성

- 데스크톱은 비교와 반복 작업에 맞춘 조밀한 레이아웃을 사용한다.
- 좁은 화면에서는 KPI와 입력 그리드를 1열 또는 2열로 줄이고 가로 넘침을 만들지 않는다.
- 긴 종목명과 한국어 단어가 버튼 밖으로 벗어나지 않게 줄바꿈 또는 최소 너비를 설정한다.
- 키보드 포커스가 항상 보이도록 하며 `Escape`로 모달을 닫을 수 있게 한다.
- 아이콘 버튼에는 `aria-label`, 입력에는 연결된 `label`, 차트 도구에는 텍스트 대체 설명을 제공한다.
- 최소 클릭 영역은 약 40px 이상을 권장한다.

## 8. 콘텐츠 규칙

- 화면 제목과 버튼은 사용자의 행동을 직접 표현한다.
- 기술 구현, 키보드 단축키, 디자인 설명을 일반 화면 본문에 노출하지 않는다.
- 금액은 `원`, 수량은 `주`, 비율은 `%`를 일관되게 붙인다.
- 날짜는 화면 문맥에 따라 `YYYY-MM-DD`, 시각은 필요할 때만 함께 표시한다.
- `현재가`, `평균단가`, `평가금액`, `실현손익`처럼 금융 용어를 임의로 바꾸지 않는다.

## 9. 구현 기준

- 라우트와 페이지 등록: `frontend/src/router/routeRegistry.tsx`
- 공통 API 호출: `frontend/src/api/apiClient.ts`
- 저장소 조합: `frontend/src/services/index.ts`
- 전역·업무 화면 스타일: `frontend/src/index.css`
- 아이콘: `lucide-react`

새 화면은 기존 클래스와 컴포넌트를 먼저 재사용합니다. 새로운 추상화는 반복되는 레이아웃이나 상태 처리의 복잡도를 실제로 줄일 때만 추가합니다.

## 10. 완료 점검

- 대상, 기준일, 상태와 주요 행동이 첫 화면에서 식별되는가
- 로딩·빈 결과·오류·비활성 상태가 구분되는가
- 모달 하단 버튼이 입력 영역이나 차트를 가리지 않는가
- 숫자, 단위, 상승·하락 색이 일관적인가
- 차트 라벨과 UI 텍스트가 겹치지 않는가
- 선택한 행·칩·계획선만 정확히 변경되는가
- 저장 후 다시 열었을 때 서버의 최신 상태가 보이는가
- 키보드와 좁은 화면에서도 핵심 작업을 완료할 수 있는가
